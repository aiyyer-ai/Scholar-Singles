var LEDPins = [ 5, 6, 7, 8 ];
var LEDDictionary = [];

for (i = 0; i < LEDPins.length - 1; i++) {
      //SET i AS LOW OUTPUT
      for (j = 0; j < LEDPins.length; j++) {
            if (i != j) {
                  if () {
                        //SET j AS HIGH OUTPUT
                  }
            }
      }
      //delay
      for (j = 0; j < LEDPins.length; j++) {
            //SET j AS INPUT
      }
}
